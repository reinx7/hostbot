import { Card, CardContent } from "@/components/ui/card";
export function MetricsCards({latest}:{latest?:{cpu:number;memoryMb:number;pingMs:number|null;uptimeSec:number|null}}){ const data=[['CPU',latest?`${latest.cpu.toFixed(1)}%`:'—','emoji-cpu'],['RAM',latest?`${latest.memoryMb.toFixed(0)}MB`:'—','emoji-ram'],['Ping',latest?.pingMs?`${latest.pingMs}ms`:'—','emoji-lightning'],['Uptime',latest?.uptimeSec?`${Math.floor(latest.uptimeSec/3600)}h`:'—','emoji-clock']]; return <div className="mt-8 grid gap-4 md:grid-cols-4">{data.map(([l,v,e])=><Card key={l}><CardContent className="p-5"><span className={`css-emoji ${e}`}/><p className="mt-3 text-sm text-white/50">{l}</p><p className="text-2xl font-bold">{v}</p></CardContent></Card>)}</div> }
